```js

const myObject = {
    1: "Hello",
    2: "Object"
}; // Dette er en hashmap!

const myString = "Hello String!"; // Dette er en string!
const myArray = ["Hello", "Array"];
console.log(typeof (myArray)); // dette er et array, men det behandles som et object!

console.log(myButton); // ? 
console.log(myString.split()); // Stringen er nå et Array
console.log(myArray.join(", This a now a string!")); // Ved å bruke join() metoden, har vi gjort om arrayet, til en string.
console.log(myArray.reverse());

console.log("1" + 1); // Ok, vi forventer enten 2, eller 11, eller en error?? But why?




newButton.addEventListener("click", () => {
    return myText.textContent = "";
})

console.log("Hello " + "{" + myObject[2] + "}[2]");
```